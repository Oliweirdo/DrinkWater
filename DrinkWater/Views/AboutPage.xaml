<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DrinkWater.Views.AboutPage"
             xmlns:vm="clr-namespace:DrinkWater.ViewModels"
             Shell.TabBarIsVisible="True"
             Title="{Binding Title}">
    
    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">#96d1ff</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

        <ContentPage.Content>
            <StackLayout Padding="20" HorizontalOptions="Center" VerticalOptions="Center">
                <Label Text="Przypominajka" FontSize="40" FontFamily="CabinSketch"
                   TextColor="DarkBlue" HorizontalOptions="Center" Padding="20"/>
                <Image Source="gow" HorizontalOptions="Center" />


            </StackLayout>
        </ContentPage.Content>



</ContentPage>
